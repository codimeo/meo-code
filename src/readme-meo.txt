Learnings :
* sprite.allHatBlocksFor(message)[0]
==> list of all blocks "When message received" with input {message}

* sprite.parent.threads.startProcess(block,null,null,null,null)
execute the block
